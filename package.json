{
  "name": "aipermission-monorepo",
  "private": true,
  "description": "Local permission gateway for AI agents.",
  "workspaces": [
    "frontend",
    "packages/mcp",
    "packages/npm-placeholder"
  ],
  "scripts": {
    "test": "make test",
    "build": "make build",
    "audit": "make audit",
    "hygiene": "node scripts/secret-scan.js && node scripts/line-ending-check.js",
    "release-check": "make release-check"
  }
}
